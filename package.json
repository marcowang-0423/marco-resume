{
  "name": "marco-resume",
  "version": "1.0.0",
  "description": "Personal resume website with RAG chatbot",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build needed for static site'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.28.0"
  }
}
